# Image tag overrides for platform components (Portal API, Context Gateway, jcctl)
# are added here once those components exist. Third-party images are pinned in
# components/*/images.yaml.
images: {}
