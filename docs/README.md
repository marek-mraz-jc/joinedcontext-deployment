# Deployment docs

The deployment guide is `../../docs/Deployment/` (installation, configuration, components, security baseline, edge routing, troubleshooting). Component-specific notes live in `components/<name>/README.md`.
