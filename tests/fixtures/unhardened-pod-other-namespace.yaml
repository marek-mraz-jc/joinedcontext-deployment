---
# Root, every capability, a writable root filesystem: one resource that every Pod Security
# policy in the chart has to report.
apiVersion: v1
kind: Pod
metadata:
  name: unhardened-pod-elsewhere
  namespace: traefik
spec:
  containers:
    - name: app
      image: nginx:1.27
      securityContext:
        runAsUser: 0
