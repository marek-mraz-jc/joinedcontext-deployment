apiVersion: apps/v1
kind: Deployment
metadata: { name: careless }
spec:
  template:
    spec:
      containers:
        - name: app
          image: nginx:latest
          securityContext:
            runAsUser: 0
