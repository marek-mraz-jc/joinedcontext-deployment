---
{{- $global := .Values.global }}
# Checklist:
# - [x] Minimal Resources
# - [x] Minimal storage -> stateless broker pods
# - [x] 1 Replica

replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: '1'
    # 1Gi: a Load take's pipeline upserts 4 000 vehicles per poll, and at 512Mi the broker was
    # OOM-killed six seconds after each restart while the runner replayed its batches.
    memory: 1Gi
