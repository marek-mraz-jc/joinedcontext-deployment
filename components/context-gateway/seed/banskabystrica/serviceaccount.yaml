apiVersion: joinedcontext.com/v1alpha1
kind: ServiceAccount
metadata:
  name: conformance
  namespace: banskabystrica
  title: { sk: "Konformančná sada", en: "Conformance suite" }
spec:
  owner: { user: "qa" }
  purpose: "The ETSI NGSI-LD conformance suite creates and deletes its own fixtures"
  roles:
    - { role: space-writer, scope: { contextSpace: ovzdusie } }
  credentials:
    - { kind: oauth-client, name: main }
