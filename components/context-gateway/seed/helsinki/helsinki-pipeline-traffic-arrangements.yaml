# One PublicAreaPermit per record of `hel-allu-traffic-arrangements`, written into the city space (T-2787).
apiVersion: joinedcontext.com/v1alpha1
kind: Pipeline
metadata:
  name: traffic-arrangements
  namespace: helsinki
  title: { en: "Temporary traffic arrangements" }
spec:
  class: auto
  # The register is updated several times a day; every six hours keeps a reader within a working day of it.
  period: 6h
  schedule: "30 */6 * * *"
  source:
    dataSourceRef: { kind: DataSource, name: hel-allu-traffic-arrangements }
  targetEndpoint: urn:ngsi-ld:Endpoint:{orgDomain}:helsinki:helsinki-all
  output:
    type: PublicAreaPermit
    mode: upsert
  quotas:
    maxMemoryMb: 128
    cpuMillicores: 100
